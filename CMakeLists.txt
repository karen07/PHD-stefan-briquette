cmake_minimum_required(VERSION 3.10)
project(briquette)
enable_language(CUDA)
add_compile_options()
add_executable(${PROJECT_NAME} src/briquette.cu)
install(TARGETS ${PROJECT_NAME} DESTINATION /usr/bin)
